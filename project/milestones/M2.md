# Milestone M2: Dual-mode authentication (device-code + app-only)

**Status:** `AWAITING_HUMAN_APPROVAL`
**Approved roadmap:** `ROADMAP.md` version `4` at `9e5ba6d2f6c2b7f7efa81dcfc415e1f787aaa458` (approved by `DECISION-015`)
**Frozen candidate SHA:** product code frozen at `98be0bc562de8f7cf52e3019715bc4cff571ad91` (the `ISSUE-0011` closeout commit). All product source (`server.py`, `auth.py`, `graph.py`, `web/`) has been unchanged since `ISSUE-0010` merged at `9d346f64422bf9bd5f89b43837a5f62f3e64d09b`; `ISSUE-0011` was documentation-only. This milestone record's own commit is metadata-only and changes no product file; each Codex launcher call targets the literal current `main` HEAD, as its own identity check requires, and that HEAD's product-file content is byte-identical to the frozen SHA above.
**Tree identity:** working tree clean; 173 tests, `py_compile` clean, `validate_repo.py` clean

## Outcome and traceability

M2 delivers dual-mode authentication: the existing device-code flow remains
the unchanged default, and a new opt-in app-only (client-credentials) mode
lets a user with their own Entra app registration sign in without their own
account. It is complete when all five issues are COMPLETE and the four
blind milestone reviews pass against one frozen candidate.

| Requirement/outcome | Completed issue | Evidence |
|---|---|---|
| Trim delegated scope to `Policy.Read.All` only | `ISSUE-0007` (merged `0c35851`) | `auth.py`; `project/handoffs/ISSUE-0007-handoff.md`; final review round 2 (zero findings, sandbox residual only) |
| App-only token acquisition in `auth.py` (no endpoint, no UI) | `ISSUE-0008` (merged `04e68ee`) | `auth.py`; `tests/test_auth.py`; `project/handoffs/ISSUE-0008-handoff.md`; final review `project/reviews/issues/ISSUE-0008-205125474389-codex.json` (zero findings, sandbox residual only) |
| `POST /api/auth/app` endpoint wiring app-only mode to the server | `ISSUE-0009` (merged `8253c1d7a754a3a967c2687c5ccc45e71794391a`) | `server.py`; `tests/test_server.py`; `project/handoffs/ISSUE-0009-handoff.md`; final review `project/reviews/issues/ISSUE-0009-7b0600f0831f-codex.json` (zero findings, sandbox residual only) |
| Sign-in card mode toggle and app-only form | `ISSUE-0010` (merged `9d346f64422bf9bd5f89b43837a5f62f3e64d09b`) | `web/index.html`, `web/app.js`, `web/style.css`; `tests/test_ui_safety.py`; `project/handoffs/ISSUE-0010-handoff.md`; final review `project/reviews/issues/ISSUE-0010-2a2d0b73e94d-codex.json` (zero findings, sandbox residual only) |
| M2 documentation finalization and dual-mode walkthrough | `ISSUE-0011` (merged `b50cbc2fb67e8066f22ab06a03f61425dbf1a9d1`) | `README.md`, `docs/security-boundaries.md`; `project/handoffs/ISSUE-0011-handoff.md`; final review `project/reviews/issues/ISSUE-0011-e878cdcd979b-codex.json` (zero findings, sandbox residual only) |

## Verification evidence

Re-run against the exact reviewed candidate `9c01749b221d6f7f2d8ff9ca6282cf9172477a3d`
(the milestone-record commit itself, not the pre-freeze product commit).
Product-file content is byte-identical to
`98be0bc562de8f7cf52e3019715bc4cff571ad91` (only `project/` governance
files differ). Both Claude general and Codex general independently ran
these checks themselves at this exact SHA during their reviews (see "Four
mandatory reviews") and got the same results reported below.

| Check | Command/method | Candidate SHA | Result | Evidence gap |
|---|---|---|---|---|
| Tests | `python3 -m unittest discover -s tests` | `9c01749b221d…` | 173 passed, exit 0 | none (Codex general/security independently hit the sandbox's loopback-socket restriction; 100 non-socket tests passed for them) |
| Compile | `python3 -m py_compile $(git ls-files '*.py')` | `9c01749b221d…` | exit 0 | none |
| Governance | `python3 scripts/validate_repo.py` | `9c01749b221d…` | passed (67 required files, incl. Markdown link/anchor validation of the new `ISSUE-0011` README sections) | none (Codex general/security could not complete this independently — no writable temp dir in the review sandbox) |
| Non-live documentation/UI smoke | server started locally; `/api/health`, both sign-in modes' page elements, and `/sample-data.json` all verified working (per `ISSUE-0010`/`ISSUE-0011` handoffs) | `9c01749b221d…` (product identical to `98be0bc`) | as expected | live in-browser interactive testing not performed — no browser-automation tool available this session; runtime secret-clearing behavior instead verified by a human-performed, Claude-guided manual walkthrough (`ISSUE-0010` handoff) |
| Live E2E (NOT an M2 gate; roadmap Verification strategy) | manual sign-in + fetch against a real tenant, either mode | — | **not performed** — remains a protected action requiring separate human approval naming the tenant; not required for M2 completion per `ROADMAP.md` | accepted residual, same as M1's `RISK-001` |

## Four mandatory reviews

Freeze commit for review purposes: `9c01749b221d6f7f2d8ff9ca6282cf9172477a3d`
(the milestone-record commit itself, after the pre-round-1 evidence-binding
fix described below), whose product-file content is byte-identical to
`98be0bc562de8f7cf52e3019715bc4cff571ad91`.

| Order | Fresh review | Report path | Reviewed SHA | Outcome |
|---:|---|---|---|---|
| 1 | Claude general | `project/reviews/milestones/M2-9c01749b221d-claude-general.md` | `9c01749b221d…` | `CHANGES_REQUIRED` |
| 2 | Codex general | `project/reviews/milestones/M2-9c01749b221d-codex-general.json` | `9c01749b221d…` | `BLOCKED` |
| 3 | Claude security | `project/reviews/milestones/M2-9c01749b221d-claude-security.md` | `9c01749b221d…` | `PASS_WITH_NOTES` |
| 4 | Codex security | `project/reviews/milestones/M2-9c01749b221d-codex-security.json` | `9c01749b221d…` | `BLOCKED` (sole basis: sandbox execution-evidence residual, `M2-SEC-001`) |

Initial peer conclusions were withheld: **yes.** Claude general and Codex
general were each launched independently, told not to read
`project/reviews/milestones/`, and neither saw the other's conclusion
before both were recorded. The same holds for the Claude security / Codex
security pair. (Codex general's own tree inspection incidentally surfaced
the retained, explicitly-superseded round-0 Codex general report for the
prior candidate `b55bf97` — that report is a different candidate's
conclusion, already disclosed as superseded in this file before Codex
general ran, not the live peer conclusion for this candidate.)

## Findings, remediation, and invalidation

**Pre-round-1 self-correction:** a round-0 Codex general review (candidate
`b55bf97ff4a4f850a21390443581e26e22f3179a`) found F-001 (medium,
`REQUIRED`): this record's Verification-evidence table bound
test/compile/governance evidence to product commit `98be0bc` rather than
the actual launcher-reviewed SHA, and "Four mandatory reviews" retained an
unresolved `<FREEZE-SHA>` placeholder. Fixed by creating candidate
`9c01749`. **Per the round-1 Codex general review's explicit correction
(F-002, below), this fix consumed the one permitted general-remediation
cycle** — the initial characterization of it as a non-countable "in-flight
correction" in an earlier version of this file was itself incorrect, per
that review, and is superseded by this text.

**Round-1 general findings (both against `9c01749`):**

- **Claude general — `CHANGES_REQUIRED`:** code and tests are clean (173
  tests pass, `py_compile`/`validate_repo.py` clean, every ROADMAP
  acceptance row traces to merged code, device-code path genuinely
  unchanged). All three blocking findings are governance-record defects,
  not product defects:
  - F-001 (medium): `ROADMAP.md` still labels M2 "PLANNED (unapproved)" in
    the M2 issue-table heading and a milestone-table cell, contradicting
    `DECISION-015`'s approval and the roadmap's own Delivery-status line.
  - F-002 (medium): `project/status/CURRENT.md` was not updated by the
    `9c01749` correction commit — it still bound the M2 candidate to
    `98be0bc` and described the round-0 (superseded) review state rather
    than the current one.
  - F-003 (medium, explicitly flagged as a human-decision item): whether
    the `b55bf97`→`9c01749` repair consumed the one permitted
    general-remediation cycle is "a gate-accounting ruling reserved for
    the human" — the same question Codex general's F-002 (below) answers
    in the strict direction.
  - Non-blocking: F-004 (low, `M2.md` never wrote the literal reviewed SHA
    — fixed in this update), F-005 (low, missing `aria-live`/
    `aria-describedby` on the app-only form, inherited from `ISSUE-0010`,
    out of M2 scope, tracked as a follow-up).
- **Codex general — `BLOCKED`:**
  - F-001 (medium, `REQUIRED`): `project/status/CURRENT.md` was stale and
    contradicted the review candidate (same defect as Claude general's
    F-002 above, independently found).
  - F-002 (high, `REQUIRED`): the milestone record incorrectly treated the
    `b55bf97`→`9c01749` repair as consuming zero general-remediation
    cycles. Per `AGENTS.md`, any relevant documentation change invalidates
    a review and the workflow permits at most one general-remediation
    cycle with no metadata-only exception — that cycle is **already
    used**. Explicit remediation instruction: "stop and request the exact
    human decision mandated by the exhausted general-remediation limit; do
    not silently create another candidate or waive the finding."

Both reviewers independently converged on the same two defects (stale
`CURRENT.md`; the cycle-accounting question), and both explicitly say the
resolution is a human decision, not something this Claude task may resolve
by producing another candidate. **No further candidate has been created.**
`CURRENT.md` is deliberately left as committed at review time (not
re-fixed here) so this record accurately reflects what the four reviews
actually assessed; see "Human decision" below for what happens next.

**Round-1 security findings (both against `9c01749`):**

- **Claude security — `PASS_WITH_NOTES`:** no critical or high findings;
  the secret-lifecycle guarantees hold under direct source and dynamic
  inspection (no path to disk/log/stderr/response/exception/`repr()`/
  console/storage/cookie/URL). Seven low/info findings, most notably
  SEC-001 (low): `_renew_app_only()` has no failure counter/backoff and
  doesn't clear the retained secret on a renewal failure, so a
  rotated/revoked secret keeps being replayed to Microsoft on every
  `/api/policies`/`/api/analysis` call while the UI shows "signed out" —
  retention silently outlives the user-visible session. Also SEC-003 (low,
  risk-candidate): `/api/auth/app` is an unauthenticated (loopback-scoped)
  credential-validation oracle and a session-supersession primitive not
  explicitly named in the `RISK-002` text the human already accepted.
  Recommended for documentation, not accepted by the reviewer. Full detail
  in the report.
- **Codex security — `BLOCKED`:** single finding, `M2-SEC-001` (medium,
  `BLOCKER` per the schema's severity field but explicitly the same
  sandbox-execution-evidence class documented throughout this project,
  not a product defect) — the read-only sandbox could not independently
  bind loopback sockets or write a governance-validator temp directory, so
  it could not reproduce the 173-test/`validate_repo.py` runs itself. No
  critical/high product finding.

- General-remediation cycles used: **`1` (maximum 1) — exhausted**, per
  Codex general's F-002 and Claude general's F-003.
- Security-remediation cycles used: `0` (maximum 2).
- Highest iteration count for any loop: `2` (round 0 + round 1; absolute
  maximum 5).
- Reviews invalidated and rerun: the round-0 Codex general review against
  `b55bf97ff4a4f850a21390443581e26e22f3179a` — superseded by the round-1
  review above; full report retained at
  `project/reviews/milestones/M2-b55bf97ff4a4-codex-general.json`. An
  earlier Claude general review also ran against `b55bf97` before the
  candidate changed; its report is retained at
  `project/reviews/milestones/M2-b55bf97ff4a4-claude-general.md` for the
  record but is likewise superseded (candidate identity mismatch) and does
  not count toward this milestone's four required round-1 reviews.
- Critical/high findings remaining: **none on product code.** All
  blocking findings across all four round-1 reviews are governance-record
  defects (`CURRENT.md` staleness, `ROADMAP.md` stale approval-status
  language) or the accepted sandbox execution-evidence limitation — no
  reviewer identified a product-code security or correctness defect.

## Documentation and release readiness

- Documentation audit: `README.md` and `docs/security-boundaries.md`
  finalized in `ISSUE-0011` — both auth modes documented, exact app-only
  prerequisite, secret lifecycle and rotation/revocation, certificate
  deferral, widened trust boundary and `RISK-002`/`RISK-005`/`RISK-006`
  named. Verified via a clean-checkout server smoke test (`ISSUE-0011`
  handoff) and Markdown link/anchor validation (`validate_repo.py`).
- Migration/rollback: `N/A (no persistence, in either auth mode)`.
- Onboarding/operations: `README.md` "Quick start" / "App-only mode
  (advanced)" / "End-to-end walkthrough" sections; `AGENTS.md` project
  commands table (unchanged from M1).
- Known limitations: `RISK-001` (device-code may be blocked by tenant
  policy — app-only mode is now an available alternative, not a fallback
  CAreview arranges), `RISK-002` (widened — local API auth boundary now
  also covers a session-retained client secret in app-only mode,
  `DECISION-014`), `RISK-004` (heuristic score, unchanged from M1),
  `RISK-005` (client secret exposed browser-side during entry, mitigated
  not eliminated), `RISK-006` (app-only token can't be scoped narrower
  than the app registration's own permissions). All are named in
  `README.md`'s "Known limitations" and `docs/security-boundaries.md`.

## Residual risks

| Risk | Severity | Treatment or exact human acceptance | Owner/review date |
|---|---|---|---|
| `RISK-001` | Medium | `DECISION-001` (deferred fallback); partially mitigated by app-only mode, not eliminated | Jay / M2 acceptance |
| `RISK-002` (widened) | Medium–high in app-only mode | `DECISION-014` (accepted as widened) | Jay / M2 acceptance |
| `RISK-004` | Low | `DECISION-001`/M1 (heuristic score, documented) | Jay / M2 acceptance |
| `RISK-005` | Medium–high | Roadmap v4 (owner-chosen UI form-field entry, accepted with implemented mitigations) | Jay / M2 acceptance |
| `RISK-006` | Medium | Roadmap v4 (documentation + UI caution only; not technically suppressible) | Jay / M2 acceptance |

## Human decision

- Decision record: not yet recorded — **awaiting the human decision this
  section requires.**
- Exact package/candidate under consideration:
  `9c01749b221d6f7f2d8ff9ca6282cf9172477a3d` (product-identical to
  `98be0bc562de8f7cf52e3019715bc4cff571ad91`).
- Result: **pending.** Per `AGENTS.md`, the milestone's one permitted
  general-remediation cycle is exhausted (both Codex general and Claude
  general independently concluded this), and both round-1 general reviews
  found required, non-product findings (stale `project/status/CURRENT.md`;
  stale "PLANNED (unapproved)" language surviving in `ROADMAP.md` from
  before `DECISION-015`). Neither reviewer found any product-code security
  or correctness defect; both Codex reviews' `BLOCKED` outcomes on the
  security side and the general side's residual concern rest on
  governance-record accuracy and the review sandbox's known
  execution-evidence limitation, not the implementation itself. This
  record presents the options rather than choosing one:
  1. **Accept M2 now**, explicitly ruling that the `CURRENT.md`/
     `ROADMAP.md` staleness and the exhausted-cycle finding are
     non-blocking record-hygiene issues to be fixed in an ordinary
     follow-up commit (not a new milestone candidate requiring a fifth
     review round), mirroring how `DECISION-012` (M1) accepted despite
     both Codex reviews being `BLOCKED` solely on the sandbox
     execution-evidence limitation.
  2. **Decline to accept yet**, treat the exhausted general-remediation
     cycle as blocking per `AGENTS.md`'s literal text ("exhausting a
     remediation limit blocks for an exact human decision"), and direct a
     specific next step (e.g., explicitly authorize one more corrective
     commit as a human-approved exception, or start a fresh governed
     milestone-repair process).
  3. Any other explicit disposition the human chooses, including
     accepting SEC-001/SEC-003 (Claude security) as named residuals or
     directing they be addressed first.

Reviews passing means only that they passed for the documented scope, SHA, and
evidence. It is not a security certification.
