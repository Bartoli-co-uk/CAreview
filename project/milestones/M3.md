# Milestone M3: React/TypeScript dashboard UI

**Status:** `ISSUES DELIVERED — MILESTONE GATE NOT RUN`
**Approved roadmap:** `ROADMAP.md` version `5`, `APPROVED` (`DECISION-029`, binds `8ea41ee`). The *delivered issues themselves* (`ISSUE-0012`/`ISSUE-0013`) were still implemented under a direct human override before this roadmap existed — `project/decisions/DECISION-024-react-frontend-build-step.md` — and roadmap v5's approval does not retroactively supply the brief/roadmap cycle they skipped; it only binds them to a milestone and approves `ISSUE-0014` as a target.
**Frozen candidate SHA:** *None.* No candidate has been frozen for a milestone review, because no milestone review has been run.
**Tree identity:** delivered work is on `main` — `ISSUE-0012` merged at `5189959392ec2331c799199f5d70457ff361a3ba`, `ISSUE-0013` fast-forwarded at `80156d32feb6f4b85debc44897d04563bb35998a`.

## What this record is, and is not

This file exists so the largest single change to CAreview's product surface
is not invisible at the milestone layer. It is **not** a milestone completion
package. Read the "Four mandatory reviews" section before treating any part of
this as an acceptance.

`M1` and `M2` each earned `COMPLETE` by passing four blind reviews against one
frozen candidate and then receiving an exact human decision (`DECISION-012`,
`DECISION-023`). M3 has done neither. Its two issues each passed the *per-issue*
Codex gate — which is a real gate, applied in full, three rounds each — but the
per-issue gate is not the milestone gate and does not substitute for it.

## Outcome and traceability

M3 replaces the hand-written vanilla-JS/HTML/CSS `web/` UI with a multi-page
React + TypeScript dashboard built by Vite, serving both a non-specialist
at-a-glance reader and a practitioner from the same data, against the
**unchanged** `/api/policies` and `/api/analysis` contract. No analyzer
capability, Graph scope, or backend data source was added.

| Requirement/outcome | Completed issue | Evidence |
|---|---|---|
| React/TypeScript dashboard replacing the vanilla `web/` UI | `ISSUE-0012` (merged `5189959392ec2331c799199f5d70457ff361a3ba`, `DECISION-025`) | `frontend/`; `project/handoffs/ISSUE-0012-handoff.md`; final review `project/reviews/issues/ISSUE-0012-195bd8e74688-codex.json` (`CHANGES_REQUIRED`, residual tracked → `ISSUE-0013`) |
| Build output served under the existing static-file allowlist | `ISSUE-0012` | `server.py` `STATIC_FILES`; `frontend/vite.config.ts` (fixed, non-hashed filenames; `emptyOutDir: false` preserves `web/sample-data.json`) |
| Untrusted tenant strings still render as text, never HTML | `ISSUE-0012` | `frontend/src/test/hostileMarkup.test.tsx` and `tests/test_ui_safety.py` — deliberately checked from both the JS and Python sides, sharing a fixture |
| No dangerous DOM/code sinks in frontend source | `ISSUE-0012` | `frontend/src/test/noDangerousSinks.test.ts` (scans `src/`) |
| Scoped, server-side device-code session abandonment | `ISSUE-0013` (fast-forwarded `80156d32feb6f4b85debc44897d04563bb35998a`, `DECISION-027`) | `auth.py` `AuthManager.abandon(handle)`; `POST /api/auth/abandon` in `server.py`; race tests in `tests/test_auth.py` and `tests/test_server.py`; `project/handoffs/ISSUE-0013-handoff.md` |
| CI runs the frontend build and tests | `ISSUE-0014` (merged `f63a0dadae917f35b328b60b1a562aa535d97d10`, `DECISION-031`) | `.github/workflows/validate.yml` now runs `npm ci`, `npm run build`, and `npm test` in `frontend/` alongside the three Python steps. Merged with an accepted sandbox execution-evidence residual (round-2 Codex review `BLOCKED`, zero content findings) |

## Verification evidence

Recorded from the `ISSUE-0013` round-2 handoff — the last point at which all
checks were run and their real output captured. **These are per-issue check
results, not milestone-candidate results:** no commit has been frozen and
re-verified as an M3 milestone candidate.

| Check | Command/method | Candidate SHA | Result | Evidence gap |
|---|---|---|---|---|
| Backend tests | `python3 -m unittest discover -s tests` | `8858858a2090…` | 188 passed | Not re-run against a frozen milestone candidate (none exists) |
| Frontend tests | `cd frontend && npm test` | `8858858a2090…` | 91 passed, exit 0 | As above. **CI now executes these too (`ISSUE-0014`, merged), though not yet re-verified against a frozen M3 milestone candidate** |
| Compile | `python3 -m py_compile $(git ls-files '*.py')` | `8858858a2090…` | exit 0 | As above |
| Governance | `python3 scripts/validate_repo.py` | `8858858a2090…` | passed | As above |
| Frontend build | `cd frontend && npm install && npm run build` | `8858858a2090…` | produced `web/index.html`, `index.js`, `index.css` | Not reproduced in CI; no build-provenance or lockfile-audit check exists (`RISK-009`) |
| Browser verification | Manual walkthrough in a real browser | per `ISSUE-0012` handoff | as expected | jsdom-based tests are not a real browser engine; no automated real-browser test exists in CI |
| Live E2E | Sign-in + fetch against a real tenant | — | **not performed** | Protected action requiring separate human approval; unchanged from M1/M2 |

## Four mandatory reviews

| Order | Fresh review | Report path | Reviewed SHA | Outcome |
|---:|---|---|---|---|
| 1 | Claude general | *not run* | — | **`NOT RUN`** |
| 2 | Codex general | *not run* | — | **`NOT RUN`** |
| 3 | Claude security | *not run* | — | **`NOT RUN`** |
| 4 | Codex security | *not run* | — | **`NOT RUN`** |

Initial peer conclusions were withheld: `N/A — no review has been conducted.`

**No report may be backfilled into this table from per-issue reviews.** The six
Codex issue reviews across `ISSUE-0012` and `ISSUE-0013` reviewed those issues'
diffs, against those issues' scope, at those issues' SHAs. They are not
milestone reviews and cannot be counted as any of the four above.

### Per-issue review evidence that *does* exist

Recorded here for traceability, explicitly not as milestone evidence:

| Issue | Round | Report | Outcome |
|---|---:|---|---|
| `ISSUE-0012` | 0 | `project/reviews/issues/ISSUE-0012-4cb61161be32-codex.json` | `BLOCKED` — F-001 (high): uncancellable device-code poll could overwrite client state |
| `ISSUE-0012` | 1 | `project/reviews/issues/ISSUE-0012-3748ff133182-codex.json` | `BLOCKED` — F-001 deeper: a stale *successful* poll leaves an orphaned server-side token with no reachable sign-out |
| `ISSUE-0012` | 2 | `project/reviews/issues/ISSUE-0012-195bd8e74688-codex.json` | `CHANGES_REQUIRED` — F-001 narrower: compensating logout was fire-and-forget and unscoped. Repair budget exhausted → `DECISION-025` |
| `ISSUE-0013` | 0 | `project/reviews/issues/ISSUE-0013-d3866851c7d6-codex.json` | `BLOCKED` — F-001 (high): `authAbandon` fire-and-forget, no retry |
| `ISSUE-0013` | 1 | `project/reviews/issues/ISSUE-0013-8c273e194622-codex.json` | `BLOCKED` — F-001: retry window too short; F-002: stale `CURRENT.md` |
| `ISSUE-0013` | 2 | `project/reviews/issues/ISSUE-0013-8858858a2090-codex.json` | `BLOCKED` — F-001: fails open after retry exhaustion; residual lacked human acceptance. Repair budget exhausted → `DECISION-027` |

Worth noting for anyone assessing M3's quality: across six rounds the reviewer
never cleared `ISSUE-0012`/`ISSUE-0013` on the merits. Both merged on an
explicit human risk acceptance with the repair budget exhausted. That is a
legitimate outcome under `AGENTS.md`, and it is a different thing from a clean
pass.

## Findings, remediation, and invalidation

- General findings: `N/A` — no milestone general review has run.
- Security findings: `N/A` — no milestone security review has run. **The
  frontend's security-relevant properties (XSS-safety via JSX escaping, CSP
  posture, the npm build-time dependency boundary) have never been assessed by
  a security review at any level** — the per-issue reviews were scoped to their
  diffs and did not treat the dependency surface at all.
- Remediation issues: `ISSUE-0013` remediated `ISSUE-0012`'s round-2 residual.
- General-remediation cycles used: `0` (maximum 1) — none available to use, no
  gate has run.
- Security-remediation cycles used: `0` (maximum 2).
- Highest iteration count for any loop: `3` per issue (rounds 0–2), within the
  absolute maximum of 5.
- Reviews invalidated and rerun: `N/A`.
- Critical/high findings remaining: **unknown — not assessed at milestone
  level.** This is an evidence gap, not a clean result, and must not be read as
  one.

## Documentation and release readiness

- Documentation audit: `README.md` (dashboard description, Quick Start build
  step, HTTP API, known limitations), `frontend/README.md` (install, build, dev
  server caveat, tests, source layout), `docs/security-boundaries.md` (scoped
  abandonment; build-time dependency boundary and rendering-safety model added
  with roadmap v5), `CONTRIBUTING.md` (frontend commands, added with roadmap
  v5). Markdown links and anchors validated by `scripts/validate_repo.py`.
- Migration/rollback: the vanilla `web/` UI remains in Git history before
  `ISSUE-0012`'s merge and can be restored if `DECISION-024` is ever revoked.
  No data migration exists — the app persists nothing.
- Onboarding/operations: **regressed, deliberately.** `git clone && python3
  server.py` no longer yields a working UI; `npm install && npm run build` must
  run first. Documented in three places; tracked as `RISK-010`.
- Known limitations: `RISK-009` (npm build-time supply chain, **accepted as
  residual at `DECISION-028`**), `RISK-010` (onboarding regression),
  `RISK-011` (abandonment fails open, accepted at `DECISION-027`), plus
  everything inherited from M1/M2. No automated real-browser test exists;
  jsdom is not a browser engine.

## Residual risks

| Risk | Severity | Treatment or exact human acceptance | Owner/review date |
|---|---|---|---|
| `RISK-009` (npm build-time supply chain) | Medium | **Accepted as residual at `DECISION-028`**, on the same low-traffic/single-user reasoning as `RISK-002`; partial mitigations (committed lockfile, no runtime external assets, no backend dependency) remain in place but were not what the acceptance itself rested on | Jay / at `ISSUE-0014`, or if scope/user base grows |
| `RISK-010` (onboarding regression) | Low | Implicit consequence of `DECISION-024`, recorded explicitly in roadmap v5; documentation-only mitigation | Jay / on build-arrangement change |
| `RISK-011` (abandonment fails open) | Low–medium | **Accepted by the human at `DECISION-027`**, exactly as documented | Jay / if abandon is revisited |

## Human decision

- Decision record: **none for this milestone.** `DECISION-024`, `DECISION-025`,
  `DECISION-026`, and `DECISION-027` govern the frontend work and the two
  issues' merges; `DECISION-029` approves roadmap v5 (which creates this
  milestone record and approves `ISSUE-0014` as a target). None of these is a
  milestone acceptance and none should be cited as one — the four-review
  milestone gate below remains entirely separate and has not run.
- Exact package/candidate approved: `N/A` — no candidate frozen, no package
  presented.
- Result: `N/A`.

### Open questions for the human

1. Should M3's milestone gate be run at all, or is the per-issue gate plus
   `DECISION-024`'s override considered sufficient for this out-of-band track?
2. ~~If it is run, should it wait until `ISSUE-0014` lands, so the milestone
   candidate is one whose frontend tests actually execute in CI?~~
   **Resolved: `ISSUE-0014` has landed** (`DECISION-031`) — any future
   milestone candidate can now be one whose frontend tests execute in CI.
3. ~~`RISK-009` needs a decision either way — accepted as residual, or treated
   by pinning/auditing work beyond what `ISSUE-0014` covers.~~ **Resolved:**
   accepted as residual, `DECISION-028`.

Reviews passing means only that they passed for the documented scope, SHA, and
evidence. It is not a security certification. In this milestone's case no
milestone review has passed, because none has been run.
