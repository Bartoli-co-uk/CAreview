# Milestone M3: React/TypeScript dashboard UI

**Status:** `REVIEWING`
**Approved roadmap:** `ROADMAP.md` version `5`, `APPROVED` (`DECISION-029`, binds `8ea41ee`). The *delivered issues themselves* (`ISSUE-0012`/`ISSUE-0013`) were still implemented under a direct human override before this roadmap existed — `project/decisions/DECISION-024-react-frontend-build-step.md` — and roadmap v5's approval does not retroactively supply the brief/roadmap cycle they skipped; it only binds them to a milestone and approves `ISSUE-0014` as a target, now `COMPLETE` (`DECISION-031`).
**Frozen candidate SHA:** round 1 was `61d76c57ff2d70fe95988497e6eaafd0b1649a41`; round 2 (this repair, fixing round 1's five governance-record findings) is this commit — the launcher records the full HEAD SHA. Product code remains frozen at `861f401` (the `ISSUE-0014` CI step-order fix — the last commit that touched any product/CI-config file); neither the round-1 freeze nor this round-2 repair changes any product/CI file, only `project/` governance records.
**Tree identity:** working tree clean; 188 backend tests, `py_compile` clean, `validate_repo.py` clean, `cd frontend && npm run build && npm test` clean (91 passed), CI green on `main` (confirmed via `gh run watch`).

## What this record is, and is not

This file exists so the largest single change to CAreview's product surface
is not invisible at the milestone layer. It is **not yet** a milestone
completion package — round 1 of the four-review gate has run (below) and
found only record-binding defects, now repaired at this commit; a fresh
round-2 set of all four reviews is required before any human acceptance
decision. Read the "Four mandatory reviews" section before treating any
part of this as an acceptance.

`M1` and `M2` each earned `COMPLETE` by passing four blind reviews against one
frozen candidate and then receiving an exact human decision (`DECISION-012`,
`DECISION-023`). M3's three issues each passed the *per-issue* Codex gate —
a real gate, applied in full, three rounds each for `ISSUE-0012`/`ISSUE-0013`
— but the per-issue gate is not the milestone gate and does not substitute
for it. The milestone gate itself began at candidate
`61d76c57ff2d70fe95988497e6eaafd0b1649a41` and is now on its repaired
candidate, this commit.

## Outcome and traceability

M3 replaces the hand-written vanilla-JS/HTML/CSS `web/` UI with a multi-page
React + TypeScript dashboard built by Vite, serving both a non-specialist
at-a-glance reader and a practitioner from the same data, against the
**unchanged** `/api/policies` and `/api/analysis` contract. No analyzer
capability, Graph scope, or backend data source was added.

| Requirement/outcome | Completed issue | Evidence |
|---|---|---|
| React/TypeScript dashboard replacing the vanilla `web/` UI | `ISSUE-0012` (merged `5189959392ec2331c799199f5d70457ff361a3ba`, `DECISION-025`) | `frontend/`; `project/handoffs/ISSUE-0012-handoff.md`; final review `project/reviews/issues/ISSUE-0012-195bd8e74688-codex.json` (`CHANGES_REQUIRED`, residual tracked → `ISSUE-0013`) |
| Build output served under the existing static-file allowlist | `ISSUE-0012` | `server.py` `STATIC_FILES`; `frontend/vite.config.ts` (fixed, non-hashed filenames; `emptyOutDir: false` preserves `web/sample-data.json`) |
| Untrusted tenant strings still render as text, never HTML | `ISSUE-0012` | `frontend/src/test/hostileMarkup.test.tsx` and `tests/test_ui_safety.py` — deliberately checked from both the JS and Python sides, sharing a fixture |
| No dangerous DOM/code sinks in frontend source | `ISSUE-0012` | `frontend/src/test/noDangerousSinks.test.ts` (scans `src/`) |
| Scoped, server-side device-code session abandonment | `ISSUE-0013` (fast-forwarded `80156d32feb6f4b85debc44897d04563bb35998a`, `DECISION-027`) | `auth.py` `AuthManager.abandon(handle)`; `POST /api/auth/abandon` in `server.py`; race tests in `tests/test_auth.py` and `tests/test_server.py`; `project/handoffs/ISSUE-0013-handoff.md` |
| CI runs the frontend build and tests | `ISSUE-0014` (merged `f63a0dadae917f35b328b60b1a562aa535d97d10`, `DECISION-031`) | `.github/workflows/validate.yml` now runs `npm ci`, `npm run build`, and `npm test` in `frontend/` alongside the three Python steps. Merged with an accepted sandbox execution-evidence residual (round-2 Codex review `BLOCKED`, zero content findings) |

## Verification evidence

Independently re-executed against the frozen **round-1** milestone candidate
`61d76c57ff2d70fe95988497e6eaafd0b1649a41` (not backfilled from any
per-issue handoff). Round 1's Codex general and Codex security reviews both
independently found this table, at the time, still bound to the stale
`ISSUE-0013` candidate SHA (`8858858a2090…`) rather than the milestone
candidate itself — real finding, fixed here. This repair commit changes no
product or CI-config file (only `project/` governance records), so the
round-1 results below remain the accurate, current state of the tree;
each fresh round-2 review independently re-runs what it can regardless.

| Check | Command/method | Candidate SHA | Result | Evidence gap |
|---|---|---|---|---|
| Backend tests | `python3 -m unittest discover -s tests` | `61d76c57ff2d…` | 188 passed, exit 0 | none |
| Frontend tests | `cd frontend && npm test` | `61d76c57ff2d…` | 91 passed, 7 test files, exit 0 | none |
| Compile | `python3 -m py_compile $(git ls-files '*.py')` | `61d76c57ff2d…` | exit 0 | none |
| Governance | `python3 scripts/validate_repo.py` | `61d76c57ff2d…` | passed (67 required files) | none |
| Frontend build | `cd frontend && npm run build` | `61d76c57ff2d…` | produced `web/index.html` (0.54 kB), `index.css` (6.56 kB), `index.js` (237.09 kB) | no build-provenance or lockfile-audit check exists (`RISK-009`, accepted `DECISION-028`) |
| CI (GitHub Actions) | `.github/workflows/validate.yml`, `validate` job | `861f401` (product/CI-config identical to `61d76c57ff2d…`) | **green** — all 10 steps succeeded, confirmed via `gh run watch`; only a non-blocking Node.js-20-deprecation annotation | Codex reviewers' sandboxes have no network access to independently query the Actions API themselves — recorded here as author-run, out-of-sandbox evidence, same class as the other sandbox-execution-evidence residuals below |
| Browser verification | Manual walkthrough in a real browser | per `ISSUE-0012` handoff | as expected | jsdom-based tests are not a real browser engine; no automated real-browser test exists in CI |
| Live E2E | Sign-in + fetch against a real tenant | — | **not performed** | Protected action requiring separate human approval; unchanged from M1/M2 |

## Four mandatory reviews

**Round 1** (candidate `61d76c57ff2d70fe95988497e6eaafd0b1649a41`):

| Order | Fresh review | Report path | Reviewed SHA | Outcome |
|---:|---|---|---|---|
| 1 | Claude general | `project/reviews/milestones/M3-61d76c57ff2d-claude-general.md` | `61d76c57ff2d…` | `CHANGES_REQUIRED` |
| 2 | Codex general | `project/reviews/milestones/M3-61d76c57ff2d-codex-general.json` | `61d76c57ff2d…` | `BLOCKED` |
| 3 | Claude security | `project/reviews/milestones/M3-61d76c57ff2d-claude-security.md` | `61d76c57ff2d…` | `PASS_WITH_NOTES` |
| 4 | Codex security | `project/reviews/milestones/M3-61d76c57ff2d-codex-security.json` | `61d76c57ff2d…` | `INCONCLUSIVE` |

Initial peer conclusions were withheld: **yes.** Claude general and Codex
general were each conducted independently and neither read the other's
report before writing its own; the same holds for the Claude security /
Codex security pair. Both Codex reviews were launched as background
processes before either Claude report was written, and both Claude
reports were written without reading `project/reviews/milestones/` first.

**Round 2 (this repair) has not yet run** — see "Findings, remediation,
and invalidation" below for why a repair was needed and what changed. A
fresh set of all four reviews against this commit is the next required
action.

**No report may be backfilled into this table from per-issue reviews.** The six
Codex issue reviews across `ISSUE-0012` and `ISSUE-0013` reviewed those issues'
diffs, against those issues' scope, at those issues' SHAs. They are not
milestone reviews and cannot be counted as any of the four above.

### Per-issue review evidence that *does* exist

Recorded here for traceability, explicitly not as milestone evidence:

| Issue | Round | Report | Outcome |
|---|---:|---|---|
| `ISSUE-0012` | 0 | `project/reviews/issues/ISSUE-0012-4cb61161be32-codex.json` | `BLOCKED` — F-001 (high): uncancellable device-code poll could overwrite client state |
| `ISSUE-0012` | 1 | `project/reviews/issues/ISSUE-0012-3748ff133182-codex.json` | `BLOCKED` — F-001 deeper: a stale *successful* poll leaves an orphaned server-side token with no reachable sign-out |
| `ISSUE-0012` | 2 | `project/reviews/issues/ISSUE-0012-195bd8e74688-codex.json` | `CHANGES_REQUIRED` — F-001 narrower: compensating logout was fire-and-forget and unscoped. Repair budget exhausted → `DECISION-025` |
| `ISSUE-0013` | 0 | `project/reviews/issues/ISSUE-0013-d3866851c7d6-codex.json` | `BLOCKED` — F-001 (high): `authAbandon` fire-and-forget, no retry |
| `ISSUE-0013` | 1 | `project/reviews/issues/ISSUE-0013-8c273e194622-codex.json` | `BLOCKED` — F-001: retry window too short; F-002: stale `CURRENT.md` |
| `ISSUE-0013` | 2 | `project/reviews/issues/ISSUE-0013-8858858a2090-codex.json` | `BLOCKED` — F-001: fails open after retry exhaustion; residual lacked human acceptance. Repair budget exhausted → `DECISION-027` |

Worth noting for anyone assessing M3's quality: across six rounds the reviewer
never cleared `ISSUE-0012`/`ISSUE-0013` on the merits. Both merged on an
explicit human risk acceptance with the repair budget exhausted. That is a
legitimate outcome under `AGENTS.md`, and it is a different thing from a clean
pass.

## Findings, remediation, and invalidation

**Round-1 general findings (both against `61d76c57ff2d…`):**

- **Claude general — `CHANGES_REQUIRED`, four findings, all governance-record
  staleness, no product defect:** F-001 `ROADMAP.md:20` still said
  `ISSUE-0014` was `PLANNED`/not started (it is `COMPLETE`, `DECISION-031`);
  F-002 `ROADMAP.md:375` still said frontend checks "do not run in CI until
  `ISSUE-0014`" (they do, as of `ISSUE-0014`'s merge); F-003 `ROADMAP.md`'s
  `RISK-009` row still said "CI does not build the frontend at all" and
  recommended `ISSUE-0014` as future work (already done); F-004
  `project/status/CURRENT.md`'s "Active milestone" row still described the
  pre-freeze M3 state instead of the frozen `REVIEWING` state, contradicting
  the row two lines above it in the same file.
- **Codex general — `BLOCKED`, one finding (F-001, medium):** this file's
  own "Verification evidence" table was bound to the stale `ISSUE-0013`
  candidate SHA (`8858858a2090…`) rather than the frozen milestone
  candidate, and the surrounding prose explicitly disclosed that gap rather
  than hiding it — but per `AGENTS.md`, stale/uncertain evidence still
  blocks regardless of the disclosure. Independently converged on
  substantively the same defect Codex security also found (below).

**Round-1 security finding (against `61d76c57ff2d…`):**

- **Claude security — `PASS_WITH_NOTES`:** no critical or high finding.
  Independently verified rendering safety (JSX escaping, hostile-markup
  tests), the scoped-abandon endpoint's origin/type checks and handle
  scoping, the CI workflow's least-privilege permissions and SHA-pinned
  actions, and confirmed `RISK-009`/`RISK-010`/`RISK-011` are accurately
  documented as already-accepted residuals. One low, non-blocking note: the
  dangerous-sink scan is text-pattern based and could theoretically be
  evaded by deliberate obfuscation — immaterial under this project's
  single-maintainer threat model.
- **Codex security — `INCONCLUSIVE`, one finding (`M3-SEC-001`, medium):**
  the same verification-evidence staleness Codex general found, examined
  from the security-evidence angle — the milestone record's claimed
  candidate-bound results (188 tests, 91 frontend tests, clean build,
  green CI) could not be reconciled with the "not re-run against a frozen
  candidate" disclosure in the same file, so mandatory security evidence
  could not be established as candidate-bound. No critical/high product
  finding; 90 independently-runnable focused security tests
  (`tests.test_auth`, `tests.test_graph`, `tests.test_ui_safety`) passed in
  the review sandbox.

**Both general reviewers and the security reviewer converged on the same
root cause**: this record's verification table and several `ROADMAP.md`/
`CURRENT.md` passages were written before `ISSUE-0014` completed and before
this candidate was frozen, and were not fully resynchronized. **This
repair (round 2) fixes all five findings** (Claude general's F-001–F-004,
Codex general's F-001, which is the same defect Codex security's
`M3-SEC-001` also names) by: rebinding the verification-evidence table to
the actual frozen candidate with fresh, real command output (see above);
correcting `ROADMAP.md`'s three stale `ISSUE-0014` passages; and
correcting `CURRENT.md`'s "Active milestone" row. No product or CI-config
file changed.

- General-remediation cycles used: **`1` (maximum 1) — exhausted**, this
  round.
- Security-remediation cycles used: `1` (maximum 2).
- Highest iteration count for any loop: `2` (round 1 + round 2; absolute
  maximum 5).
- Reviews invalidated and rerun: this repair creates a new candidate per
  `AGENTS.md` — round 1's four reports above remain on record but are
  superseded; all four reviews must run fresh against the new candidate
  (this commit) before any human decision.
- Critical/high findings remaining: **none.** Every round-1 finding across
  all four reviews was a governance-record defect (verification-table
  binding, stale roadmap/status prose) or an accepted sandbox
  execution-evidence limitation. No reviewer identified a product-code
  security or correctness defect.

## Documentation and release readiness

- Documentation audit: `README.md` (dashboard description, Quick Start build
  step, HTTP API, known limitations), `frontend/README.md` (install, build, dev
  server caveat, tests, source layout), `docs/security-boundaries.md` (scoped
  abandonment; build-time dependency boundary and rendering-safety model added
  with roadmap v5), `CONTRIBUTING.md` (frontend commands, added with roadmap
  v5). Markdown links and anchors validated by `scripts/validate_repo.py`.
- Migration/rollback: the vanilla `web/` UI remains in Git history before
  `ISSUE-0012`'s merge and can be restored if `DECISION-024` is ever revoked.
  No data migration exists — the app persists nothing.
- Onboarding/operations: **regressed, deliberately.** `git clone && python3
  server.py` no longer yields a working UI; `npm install && npm run build` must
  run first. Documented in three places; tracked as `RISK-010`.
- Known limitations: `RISK-009` (npm build-time supply chain, **accepted as
  residual at `DECISION-028`**), `RISK-010` (onboarding regression),
  `RISK-011` (abandonment fails open, accepted at `DECISION-027`), plus
  everything inherited from M1/M2. No automated real-browser test exists;
  jsdom is not a browser engine.

## Residual risks

| Risk | Severity | Treatment or exact human acceptance | Owner/review date |
|---|---|---|---|
| `RISK-009` (npm build-time supply chain) | Medium | **Accepted as residual at `DECISION-028`**, on the same low-traffic/single-user reasoning as `RISK-002`; partial mitigations (committed lockfile, no runtime external assets, no backend dependency) remain in place but were not what the acceptance itself rested on | Jay / at `ISSUE-0014`, or if scope/user base grows |
| `RISK-010` (onboarding regression) | Low | Implicit consequence of `DECISION-024`, recorded explicitly in roadmap v5; documentation-only mitigation | Jay / on build-arrangement change |
| `RISK-011` (abandonment fails open) | Low–medium | **Accepted by the human at `DECISION-027`**, exactly as documented | Jay / if abandon is revisited |

## Human decision

- Decision record: **none for this milestone.** `DECISION-024`, `DECISION-025`,
  `DECISION-026`, and `DECISION-027` govern the frontend work and the two
  issues' merges; `DECISION-029` approves roadmap v5 (which creates this
  milestone record and approves `ISSUE-0014` as a target). None of these is a
  milestone acceptance and none should be cited as one — the four-review
  milestone gate below remains entirely separate and has not run.
- Exact package/candidate approved: `N/A` — no candidate frozen, no package
  presented.
- Result: `N/A`.

### Open questions for the human

1. ~~Should M3's milestone gate be run at all, or is the per-issue gate plus
   `DECISION-024`'s override considered sufficient for this out-of-band track?~~
   **Resolved: the human directed running it** — this file's round-1 review
   cycle is the result.
2. ~~If it is run, should it wait until `ISSUE-0014` lands, so the milestone
   candidate is one whose frontend tests actually execute in CI?~~
   **Resolved: `ISSUE-0014` has landed** (`DECISION-031`) — the candidate
   round 1 reviewed is one whose frontend tests execute in CI.
3. ~~`RISK-009` needs a decision either way — accepted as residual, or treated
   by pinning/auditing work beyond what `ISSUE-0014` covers.~~ **Resolved:**
   accepted as residual, `DECISION-028`.

**Still open:** a fresh round of all four reviews against this repaired
candidate. No human acceptance decision should be made until that round
completes, per `AGENTS.md`'s invalidation rule.

Reviews passing means only that they passed for the documented scope, SHA, and
evidence. It is not a security certification. In this milestone's case no
milestone review has passed cleanly yet — round 1 found only record-binding
defects, now repaired; round 2 has not yet run.
